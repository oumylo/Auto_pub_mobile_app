import 'package:flutter/material.dart';

class PublierCampagnePage extends StatelessWidget {
  const PublierCampagnePage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Publier une Campagne', style: TextStyle(fontSize: 20)),
    );
  }
}
