import 'package:flutter/material.dart';

class MesCampagnesConducteurPage extends StatelessWidget {
  const MesCampagnesConducteurPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Mes Campagnes Conducteur', style: TextStyle(fontSize: 20)),
    );
  }
}
