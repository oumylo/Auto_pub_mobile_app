import 'package:flutter/material.dart';

class ValidationCampagnePage extends StatelessWidget {
  const ValidationCampagnePage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Liste des Campagnes', style: TextStyle(fontSize: 20)),
    );
  }
}
