import 'package:flutter/material.dart';

class SuiviCampagnePage extends StatelessWidget {
  const SuiviCampagnePage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('suivi des Campagnes', style: TextStyle(fontSize: 20)),
    );
  }
}
